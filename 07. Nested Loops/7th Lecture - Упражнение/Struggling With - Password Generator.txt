/* 

if (fifthSymbol > lastValueOf5th) {
    lastValueOf5th = fifthSymbol;
}
if (lastValueOf5th <= firstSymbol && lastValueOf5th <= secondSymbol) {
    continue;
} else {
    if (fifthSymbol >= lastValueOf5th) {
        concat += `${firstSymbol}${secondSymbol}${String.fromCharCode(thirdSymbol)}${String.fromCharCode(fourthSymbol)}${lastValueOf5th} `;    
    } else {
        continue;
    }
}

*/



/*

if (fifthSymbol !== firstNum && countOfLastSymb !== firstNum) {
    if (fifthSymbol <= firstSymbol && fifthSymbol <= secondSymbol) {
        countOfLastSymb++;
        continue;
    } else {
        countOfLastSymb++;
        concat += `${firstSymbol}${secondSymbol}${String.fromCharCode(thirdSymbol)}${String.fromCharCode(fourthSymbol)}${fifthSymbol} `;
    }
} else {
    if (countOfLastSymb === firstNum) {
        concat += `${firstSymbol}${secondSymbol}${String.fromCharCode(thirdSymbol)}${String.fromCharCode(fourthSymbol)}${lastValueOf5th} `;
        break;
    }
}

*/




/*
    if (fifthSymbol <= firstSymbol && fifthSymbol <= secondSymbol) {
        continue;
    } else {
        concat += `${firstSymbol}${secondSymbol}${String.fromCharCode(thirdSymbol)}${String.fromCharCode(fourthSymbol)}${fifthSymbol} `;
    }

*/